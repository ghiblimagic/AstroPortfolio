---
const { src, alt, label, styling } = Astro.props;
import ButtonALink from "@/components/buttons/ButtonALink.astro";
---

<div class="my-6 p-4 ">
  <img src={src} alt={alt} class="rounded mb-3" />

 
  <div class="text-center">
  <ButtonALink href={src} label={label} styling={styling} download />
 <!-- In Astro, download=true does NOT pass a boolean. it passes true as a string. So just do download to get true passed -->
  </div>

</div>
